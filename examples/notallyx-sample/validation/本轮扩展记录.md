# 2026-09-07 标签与批量操作扩展

状态：标签 Intent 与冻结 Script 的 R9 真机扩展已通过；UI、数据库、产物哈希和真实修改调用均已独立复核。用户换机后当前独占 OPPO `FYZLAU49X8OVQGJ7`（PGFM10，Android 16/SDK 36）；旧设备 `b46093e6` 的记录单独保留。隔离包 `io.github.mobileaidev.notallyx.sample`。本轮不修改 R5/R6/R7 的原始结果和冻结文件。旧三轮成功只适用于其各自 APK、Host 和固定核心套件；不是新候选版本的全 App 验收。

## 已发现并修复的业务缺陷

旧迁移版重复创建 `AAB扩展标签-01` 时发生真机崩溃。Intent `intent-1788751052004-2` 的 `15-act.json` 返回动作成功，随后 `16-observe.json` 出现 socket hang up；2026-09-07 11:20:19.413 的设备崩溃日志明确记录 `IllegalStateException: Label already exists: AAB扩展标签-01`，来自 `NoteApplicationService.addLabel`。动作回执不能代替业务判定。

原因是服务抛 `IllegalStateException`，既有 UI 回调只处理 `SQLiteConstraintException`。修复只调整重复新增与冲突改名的明确异常合同，未添加泛 catch。重复新增由上游 IGNORE 的无提示 no-op 变为使用既有错误提示；此行为变化单独列于 [LABEL_CONTRACT_FIX.md](LABEL_CONTRACT_FIX.md)。

- 真实 Room 与 BaseNoteModel 测试：修复前 5 项中 2 项失败，修复后 5/5；包括重复新增、冲突改名、全 folder 引用更新、同名改名和 SQL 触发器失败回滚。
- 完整 App 测试：199/199，未重跑 SDK 测试。
- 新 APK：`build/ai_app_bridge_artifacts/notallyx-migration/candidate-v2/notallyx-architecture-candidate-v2.apk`。
- 新 APK SHA256：`b4fe597c6154b5fdd119746226b235e7a207fa221cefde49cdb355053967c233`。
- 覆盖安装前后，10 条既有笔记、4 个标签、偏好及附件 canonical 零差异。快照分别为 `9e1ee941-0d05-476d-8b62-d0067f6baae9`、`37d5634b-9192-445c-8cfa-1abd9fa8efab`。

修复后的重复新增与冲突改名已在新 OPPO 实测：均提示“标签已存在”，App 保持运行，前后完整数据库 canonical 零差异。SQL 回滚测试也不证明 SQL 与 SharedPreferences 跨存储的断电原子性。

## Intent 能力扩充

标签列表每行拥有相同的编辑/删除按钮标识，批量选择则需要长按。已有单字段 selector 与点击不足以完整操作这两类交互。本轮新增显式 native 长按和带唯一锚点、明确祖先及直接父容器的作用域 selector；不以第一个同名按钮、全局回退或隐藏坐标旁路代替语义选择。Host 完整测试 634/634，新增 10 项长按与 9 项作用域选择器测试通过，并经独立审查。OPPO 上已通过作用域按钮的正常改名、冲突改名、删除取消与确认；长按进入多选、添加第二条并取消也已执行。此处只证明这些明确动作，不扩展为所有批量业务命令通过。

已有 Intent 的完整操作历史继续保留。弹窗键盘展开期间使用了旧位置的一次输入明确拒绝，重新观察后才成功；拒绝记录不重写为成功。

这三份新历史共 69 条 Host 记录已在临时副本中逐条核对 checksum，并 close/reopen 再读一致；原目录文件 SHA 未改变。记录完整性通过不改变其中业务崩溃、输入拒绝和连接失败的结论。复核结果保存在 `expansion-host-checksum-review.json`。

## 旧设备阻塞与换机续测

独占手机后仍发生间歇性 ADB shell 超时。单设备重连随后报 `LIBUSB_ERROR_NO_DEVICE`；Mac USB 注册表仍能看到该序列号，但 ADB 已找不到设备，单设备 attach 也失败，已请求物理拔插。重连前短探测一度正常，因此不能声称重连已经修好根因。启动阶段另有一次 ART/ADB-JDWP 原生崩溃日志，尚未归因；它与已定位的标签 Java 崩溃分开记录。

用户换成新 OPPO 后继续执行，未把旧设备连接问题记为已修复。新设备初始样例数据库为空，安装 candidate-v2 后原固定核心套件 R8 已通过。标签负例、改名、删除取消与确认及重启观察已完成，扩展固定 Script 已冻结并在 R9 重放通过。备份恢复的源码入口与 ZIP 格式已审查，但还没有真机验收；恢复是合并导入与内容去重，不是覆盖数据库，详见本地源码审查记录。

本轮原始证据：`build/ai_app_bridge_artifacts/notallyx-migration/expansion/` 和 `intent-exploration/1788750907051/`。测试、崩溃与连接日志：`.tools/business-app-migration-2026-09-07/`。标签脚本计划见 [LABELS_SUITE_PLAN.md](LABELS_SUITE_PLAN.md)。完整分母仍为 51 功能、143 模板、396 覆盖槽；旧设备记录不增加通过模板，新 OPPO 的新增结果见下文 R9。

## 新 OPPO 的实测证据

- R8：84.319 秒，27 个 UI 检查点、8 份数据库快照、61 次实际修改调用；独立复核全部通过，436 个 artifact SHA 一致。仍仅完整通过 4 个核心模板、3 个组织模板部分覆盖。新设备、初始数据、APK 和 Host 与 R5–R7 不同，不参与原三轮性能对照。
- 标签 Intent：新建近似名称 a/ab，分别赋给文字笔记/清单；重复新增和冲突改名拒绝且全库不变；a 正常改名只改变目标实体与精确引用；删除取消全库不变；确认删除后管理页、导航和两个编辑器结果正确，随后重启复查。
- 原始轨迹目录 `intent-exploration/1788752615378/`，标签范围 1–140；对应新截图和停机数据库在 `expansion-FYZLAU49X8OVQGJ7/`。1–140 中保留错误 selector 被拒绝、Recycler 动画导致节点歧义被拒绝的真实失败，不把整段历史改写为一次完成。
- 长按多选范围 141–150，`longpress-two-selected.png` 显示两条选中。`longpress-cancel-snapshot` 已与之前完整状态比较，独立读取后 canonical 零差异（不忽略字段）；未验证批量归档、删除、标签修改等命令。
- 标签历史的冷启动命令已在本任务工具输出观察到 `LaunchState: COLD`，但没有单独保存 shell launch 文件。原始强停采集、Intent 新 epoch 和重开 UI 保留；新固定 Script 必须保存本轮 launch 回执，不能用历史 goal 文字代替。

新版本固定 Script 的通过数与耗时以 R9 新输出目录报告为准。原始失败、旧安装和 R5–R8 均保留。

独立复核补充：确认删除只改变目标标签实体与 note1 的引用；note2 全字段、两个正文/任务状态/时间戳及其它标签顺序保持。该业务实现同时把隐藏标签偏好 `labelsHiddenInNavigation` 从缺省写为 `[]`，这是明确预期变化，不写成偏好字面零差异。删除后与重启后的完整 canonical 一致。另本目录 10 个 Intent 的 412 条 Host 记录在临时副本逐条 checksum 及 close/reopen 校验全部通过，原始目录 SHA 未变；其中两个 failed 操作仍保留失败结论。

仍待后续业务覆盖：批量标签当前由 UI 根据旧快照逐条提交，并非一次整批事务；导入数据若含重复标签引用，当前 Kotlin `labels - value` 只删除首次出现，仍需专门故障样本与修复验证。本轮 UI 创建的标签引用唯一，不据此宣称异常导入数据也已通过。备份导入、附件、加密与目录迁移继续保留未验收。

长按独立复核通过：操作 `intent-1788754881362-10` 的 24 条 envelope checksum 有效；实际机械回执为坐标 (540,479)、持续 700ms、dispatched=true、ambiguous=false，未声称 SDK 侧 actionId。prepared→receipt→新观察顺序一致，多选计数 1→2，Back 后退出选择。32 个输入文件 SHA 不变，复核报告见 `.tools/business-app-migration-2026-09-07/native-longpress-device-review.json`。

## R9 固定 Script 真机结果

冻结 Script `28b84805515a0fb9645857d4f7fd0ca36785919e4fa2ea6e9eb363fe9f977edd`，candidate-v2 APK 与 OPPO 身份同上。运行 `script-1788755934374-6eea39` 用时 **187.172 秒（约 3 分 7 秒）**，14 个阶段、59 个 UI 检查点、15 次数据库采集全部完成，Host 记录的实际修改调用为 121 次。安装未重做；从两条既有笔记开始，每轮用唯一 runId 创建新笔记/标签。该轮与 R8 的套件不同，不能据耗时直接算回归变慢比例。

本轮完整通过 7 个模板：文字正常/重启、清单层级正常/重启、标签管理正常/异常/重启。报告同时保留 2 个部分覆盖、134 个未运行模板。311 个参数化变体仍未运行；396 个展开槽为 7 passed、2 inconclusive、387 not_run。全 App 判定仍为 false。

独立 UI 复核已重算 59/59，并从实际 executions、required oracle 及冻结清单重建完整分母；277 个复核输入 SHA 不变。两项审查发现已修复并由离线 61/61 验证：输入未生效不能冒充重名拒绝；不可操作或未知顶层窗口不能退到背景页面做通过判定。独立数据库复核也已完成：15/15 真实 SQLite 快照、7 项标签变更/不变判定通过；原有 2 条笔记在全部快照中逐字段保留，最终 4 条。894 个产物条目 SHA 全部吻合，893 个去重复核输入前后不变；14 个阶段的 121 次真实 Host 修改调用均有唯一开始、回执和完成关联。详见 `.tools/business-app-migration-2026-09-07/r9-independent-db-hash-review.json`。

原始报告：`build/ai_app_bridge_artifacts/notallyx-migration/script-oppo-labels-r9/report/report.html` 与 `report.json`。R5–R8 和本轮探索失败历史均未覆盖。标签历史来源单独冻结为 v2，225 个文件；它用于说明 Script 从哪里来，本轮业务是否正确仍由 R9 新采集证据判断。

R9 持久记录最终复核：在已关闭原 Host FactStore 的临时副本中，270/270 记录 checksum 有效，121 次修改的 prepared/receipt 与动作 hash 对应，14 个阶段具有 durable completed；关闭重开所得记录完全相同，原 store 文件 SHA 不变。当前汇总与复核报告引用已写入 `expansion-FYZLAU49X8OVQGJ7/progress-manifest.json`，旧设备的 progress-manifest 保持历史原样。
