# 固定夹具与跨应用 Intent 验证

日期：2026-09-07。设备 OPPO PGFM10 / Android 16 / `FYZLAU49X8OVQGJ7`，隔离包 `io.github.mobileaidev.notallyx.sample`。本轮继续使用 candidate-v4，安装包 SHA-256 `0ce2d16f09b187007fe3d53c50d5a0c5b1c59ff9ba17fb64e53fb6017fff80ba`，没有改动 App 源码或重新构建 APK。

本阶段处理两个问题：Intent 在 App 与系统文件选择器之间保持明确的观察和动作归属；扩展 Script 从固定数据起跑，并在成功后恢复同一份数据，避免笔记和标签不断累积。备份流程的 Intent 证据已补齐，尚未加入固定 Script 的 18 阶段。

Intent 新增显式 `target.foregroundPackages`，本机配置为 `["com.coloros.filemanager"]`。每次观察核对前台组件；原 App 使用 native，指定的系统 App 使用 UIA。动作继承已提交观察的 provider，并在派发前再次核对前台。未指定的包、观察期间前台变化、重复选择器及非法 XML 字符引用均明确拒绝。前台变化可在同一个 operation 中调用 `observe` 后继续。UIA 原始 XML、摘要与动作选择器共用单次解码逻辑，支持文件名中的 `&#10;` 换行。接口用法见 [INTENT_FOREGROUND.md](../../../desktop/ai-app-bridge-cli/docs/INTENT_FOREGROUND.md)。

真实备份流程和独立数据核验如下：

| 检查 | 结果 |
|---|---|
| 导出 ZIP，经 OPPO 文件选择器保存到 Documents | 已取得实际文件并验证 CRC、SHA、内嵌 SQLite；10 条笔记、11 个标签全字段与导出前一致 |
| 选择新 ZIP 后取消导入 | 数据库、偏好、附件清单 canonical 零差异 |
| 将受控清单移入回收站并永久删除 | 笔记 10 → 9，仅删除 id 12；其余字段不变 |
| 精确选择新 ZIP，保留重复检查并导入 | 笔记 9 → 10，缺失清单恢复为 id 13；除新分配的主键外，全字段与备份一致 |
| 其余记录与冷启动 | 其余九条、11 个标签及偏好未改变；重启后数据零差异，页面显示父子缩进和 Parent-B 勾选 |

新 ZIP 为 `NotallyX Backup 2026-09-07 15-44.zip`，37,002 字节，SHA-256 `86ae3f9e1f96db52d43300a27c217d224d4f05c7d2951571293a5e6063d003a8`。旧 13-41 ZIP 不含本次恢复清单。产物位于 `build/ai_app_bridge_artifacts/notallyx-migration/intent-backup-oppo-r2/`：`exported-zip/oracle.json`、`restore-final-oracle.json`、`evidence-review.json` 与 `durable-review/review.json`。

可直接查看 [系统导出选择器](../../../build/ai_app_bridge_artifacts/notallyx-migration/intent-backup-oppo-r2/export-picker.png)、[永久删除后的空回收站](../../../build/ai_app_bridge_artifacts/notallyx-migration/intent-backup-oppo-r2/deleted-empty.png)、[ZIP 恢复并冷启动后的清单](../../../build/ai_app_bridge_artifacts/notallyx-migration/intent-backup-oppo-r2/restored-cold-ui.png)。

Intent 证据已独立重读：79 份冻结 Host 文件 SHA 相符，236 条持久记录在关闭后的副本中读取、校验并再次重开通过；71 份摘要从原始树重建一致，31 条动作回执与观察、决策、派发标记关联一致，其中 5 次系统 UIA 点击使用直接 ADB 输入。11 组截图均检查文件 SHA、前台组件及前后可见节点语义/位置一致，并逐张查看。快照的 sequence 是采集检查点序号，实际动作身份以回执为准。

第一条 Intent `intent-1788766367137-1` 在后续页面动画尚未结束时提前选择清单，返回 `native_selector_not_found`，未派发该动作；失败历史保留。第二条 `intent-1788768102230-2` 完成受控删除、系统选文件、恢复和冷启动验证。事件 capture 在强停后的 runtime epoch 变化下仍为 partial/unavailable，本次业务通过依据是 UI、实际 ZIP 和独立 SQLite，不能据此声称 capture 流完整。当前前台组件校验也不等于同一 Activity 内的布局原子性。

固定夹具工具为 [fixed-fixture.js](fixed-fixture.js)。它要求明确的设备、APK 和新输出目录，核对手机实际 APK；只接受无附件、提醒、常驻通知和自动备份副作用的私有样例数据。恢复前备份当前状态，暂存整套 DB/WAL/SHM/偏好并逐文件校验，替换后独立读取并比较全部业务字段。失败保留现场和旧数据快照，不自动重试或宣布成功。四文件替换不是跨文件原子事务，尚未验证掉电恢复。

已捕获夹具 `fixed-fixture-oppo-v1/fixture.json`：10 条笔记、11 个标签，公共赋值标签明确为 `AAB-label-script-1788765096973-89fdb3-ab`。它在本夹具中位于导航首位，加入两枚测试标签后仍可见。该控制器只负责测试数据准备与成功后的还原；Script 本身继续通过 UI 操作产生本轮证据。

从仓库根目录运行，`--out` 每轮使用新的目录：

```bash
node examples/notallyx-sample/validation/fixed-fixture.js run \
  --serial FYZLAU49X8OVQGJ7 \
  --apk build/ai_app_bridge_artifacts/notallyx-migration/candidate-v4/notallyx-architecture-candidate-v4.apk \
  --fixture build/ai_app_bridge_artifacts/notallyx-migration/fixed-fixture-oppo-v1/fixture.json \
  --out build/ai_app_bridge_artifacts/notallyx-migration/script-oppo-fixed-next
```

Host 全套测试 646/646；验证工具 69/69。新增测试覆盖 native/UIA 往返、前台竞态及重新观察、未允许的包、错误包的树、重复选择器、字符引用解码与固定夹具的数据保护。完整日志在 `.tools/notallyx-next-stage-2026-09-07/{host-suite,validation-suite}.log`。之后仅修改命令帮助说明，单独重跑 command-router 测试通过；Intent 冻结的旧帮助说明和后续 Script 的新帮助说明在各自 manifest 中分别保留。

固定夹具连续两轮均完成，期间未修改 Script、Host、控制器或 APK：

| 指标 | R13 | R14 |
|---|---:|---:|
| Script 回归时间（含操作、等待、采证、判定） | **311.190 秒** | **317.604 秒** |
| 包含准备和恢复的总时间 | 329.134 秒 | 334.421 秒 |
| 完成阶段 | 18 | 18 |
| UI 检查点 / 数据库快照 | 76 / 19 | 76 / 19 |
| Host 动作数 | 219 | 221 |
| 校验通过的产物 SHA | 1,365 | 1,365 |
| 持久记录读取及再次重开校验 | 474 | 478 |
| 回归前 → 回归结束 → 恢复后的笔记数 | 10 → 12 → 10 | 10 → 12 → 10 |
| 回归前 → 回归结束 → 恢复后的标签数 | 11 → 13 → 11 | 11 → 13 → 11 |

两轮回归时间相差 6.414 秒。R14 的 `list-restart` 与 `assignment-cancel` 各多一次滚动，动作历史可核对；脚本会根据实际页面继续定位目标。两轮完整初始 canonical 数据一致，每轮成功后恢复为相同基线，手机最后重新打开了样例 App。两次运行证明该夹具可重复使用，尚不构成长期稳定性或 P95 统计。

身份核对：Script SHA-256 `d5a7fe4ee00e1f23ee73f1f8c39ce81defde7876790da4195319eca3ef22e2df`；两轮 Host manifest SHA-256 `b05bb98a10489e10216f2fc4bd2a47bc676532931f43bca2b729b21626ea9525`；固定夹具 SHA-256 `88f1377c17d05d2ce1e9c6adf569585fd498de77f285bf28b71b2ea38e8bf33a`。R13 runId 为 `script-1788769206888-03a1b6`，R14 为 `script-1788769563449-4a45bd`。

每轮均另做离线复核：重新读取 19 份真实数据库及其采集依赖，重算 76 个 UI 判定，核对 1,365 项文件 SHA，关闭后的 FactStore 副本校验并再次重开。每轮另逐张查看四张三态标签弹窗截图，初始为两枚混合状态标签及未选公共标签；修改后公共标签选中、测试 ab 标签清空，项目标签保持混合。视觉核对不改写冻结报告中仍为 inconclusive 的完整标签赋值模板。

汇总在 `fixed-fixture-oppo-v1/repeatability-review.json`。两轮各自目录 `script-oppo-fixed-r13/`、`script-oppo-fixed-r14/` 保留 `result.json`、`archive-review.json`、`visual-review.json`、`durable-review/review.json`，以及 `prepare/`、`restore/` 前后快照和 `regression/report/report.{json,html}`。这些目录均位于 `build/ai_app_bridge_artifacts/notallyx-migration/`。

离线复核入口保存在 `.tools/notallyx-next-stage-2026-09-07/`：`review-intent-evidence.js` 核对跨应用观察/动作/截图，`review-restore.js` 核对 ZIP 恢复后的业务数据，`review-fixed-regression.js` 核对单轮归档，`review-durable.js` 核对持久记录，`review-isolation.js` 核对跨轮夹具一致性。R11/R12 的历史数据、时间及结论未改写；它们与本轮夹具和 Host 不同，不用于受控性能对比。

当前固定 Script 的完整分母仍为 143 模板：7 passed、4 partial/inconclusive、132 not_run；311 参数化变体未运行，完整 App / 整机验收仍为 false。下一步优先把本次验证的 ZIP 导出、取消、真实恢复流程固化进 Script，再扩展加密备份、错误密码、损坏文件和权限取消等分支，并补上跨冷启动 capture 的 epoch 证据链。先扩展可独立判定的业务场景，再扩大平台与整机范围。
