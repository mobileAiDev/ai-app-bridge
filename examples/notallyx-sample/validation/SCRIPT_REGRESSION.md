# Script regression candidate

This candidate executes text creation, exact Chinese/multiline input, save/reopen, same-note edit, and force-stop/relaunch read-back. It creates a four-item list, checks Parent-B, indents two children, edits one child, removes/restores indentation, saves/reopens, and checks persistence after restart. Label management adds two distinct names with a shared prefix, assigns them to separate notes, rejects duplicate creation and conflicting rename, renames one label, cancels deletion, confirms deletion, and checks navigation and both notes after restart. It attempts seven complete templates: `note.text_crud.normal/restart`, `list.hierarchy.normal/restart`, and `labels.manage.normal/negative/restart`.

The report retains all **51 features / 143 templates / 311 variants / 396 atomic coverage slots**. Each restart case explicitly shares its normal prefix before a separate restart/read-back. The earlier single-note label/pin/color flow keeps its original partial attempt. Full label management uses a separate attempt 2, so its new required oracles do not overwrite or conflict with that partial history. `labels.assignment.normal` and `note.pin_color.normal` remain partial: two-note assignment here does not exercise the multi-select tri-state dialog or removal, and the pin/color fragment does not cover unpin/re-pin and custom-color replacement. Nothing in this candidate claims full-app acceptance.

R9 completed on OPPO PGFM10 / SDK 36 as `script-1788755934374-6eea39`: **187.172 seconds**, 14 phases, 59 UI checkpoints and 15 database snapshots. The runner returned core success and exit 0; full-App `report.ok` remains false. Independent archived UI replay passed 59/59 and independently reconstructed the same denominator: 7 passed / 2 inconclusive / 134 not-run templates, all 311 variants not run, and 7 passed / 2 inconclusive / 387 not-run atomic slots. Only `labels.manage` is a fully passed feature. No installation was performed in this run; the existing two notes and two labels were preserved as the starting fixture. The measured interval excludes historical authoring and final JSON/HTML report writes, and is not an Agent/model timing comparison.

R9 evidence is under `build/ai_app_bridge_artifacts/notallyx-migration/script-oppo-labels-r9/`; independent UI/coverage review is `.tools/business-app-migration-2026-09-07/r9-independent-ui-coverage-review.{json,md}`. The exact executed identity is:

| Artifact | SHA-256 |
| --- | --- |
| candidate-v2 APK | `b4fe597c6154b5fdd119746226b235e7a207fa221cefde49cdb355053967c233` |
| Script | `28b84805515a0fb9645857d4f7fd0ca36785919e4fa2ea6e9eb363fe9f977edd` |
| Host code manifest | `3b78cb186aa54c9abfacc0128ee3a9d02bf386a07d42863db893faf43fccaece` |
| Original report JSON | `51351fa91cae557fba1ca1788be2dcb33d6c08c0f5f32146d2cdc8ad9c81379d` |

Independent follow-up reread all 15 SQLite/WAL snapshots and recomputed all seven label transitions/equality checks. The original two notes remained field-for-field unchanged in every snapshot; the final database has four notes. All 894 artifact entries matched their hashes, and 893 distinct reviewed input files were unchanged before/after review. All 121 Host mutation starts, receipts and completions linked correctly across 14 phases; the 59 checkpoints comprise 55 post-action observations and four explicit initial observations after cold launch. A temporary copy of the closed Host store passed 270/270 durable-record checksums, action-spec linkage for the 121 prepared/receipt pairs, 14 completed terminal records and close/reopen comparison, while the original store hashes remained unchanged. These were read-only archive reviews with no device commands. Results are `.tools/business-app-migration-2026-09-07/r9-independent-db-hash-review.json` and `r9-independent-host-durable-review.json`; their replay scripts are `review-r9-db.js` and `review-r9-host-durable.js` in the same directory.

```sh
node examples/notallyx-sample/validation/run-regression.js \
  --serial FYZLAU49X8OVQGJ7 \
  --apk /absolute/path/to/the-installed-sample.apk \
  --out /absolute/path/to/a-new-empty-run-directory
```

The device must be reserved for this run. `--serial`, `--apk`, and `--out` are required. Optional `--install` installs the verified isolated APK first and records installation time separately. `--aapt2` overrides local Android build-tools discovery; `--adb` overrides the adb executable. APK metadata must identify `io.github.mobileaidev.notallyx.sample`, and installed base APK bytes must exactly match the supplied APK hash. This runner never clears app data. New note/list titles and all new labels contain the run ID, and prior note rows must survive unchanged. R9's initial fixture has two existing notes and two labels; its current Chinese UI uses the default five visible navigation-label slots. Arbitrary language changes, hidden labels, or a crowded/collapsed label navigation are not covered by this candidate.

Before selecting or capturing an overview card, the Script waits for stable observations without duplicate target titles. Bounded swipes within the freshly observed MainListView Recycler then reveal the entire target card. Each direction permits at most eight swipes and stops on an unchanged observation; target centers and swipe endpoints remain in the observed viewport. After deleting a label, a fresh card checkpoint also requires the old labels to be absent before a note tap. This addresses the real Intent failure caused by transitional duplicate Recycler nodes without re-dispatching an ambiguous tap.

`regression-script.js` is frozen and passed as a real Script `sourcePath`; the Node Script child calls `ctx.call` and strong `ctx.assert` with current Host-issued tree evidence. It does not invoke Intent or batch. Host orchestration explicitly force-stops the sample, collects a quiescent SQLite/WAL/SHM/preferences snapshot, launches it, and starts the next Script phase. These transitions and evidence collection count toward the ten-minute regression budget.

Each checkpoint archives tree → screenshot → second tree and action/operation identity. The two trees must retain the same visible business values, geometry, Activity and window metadata around capture. The screenshot's foreground Activity must match both. Actual PNG bytes must match the SHA-256 produced at Host capture time and retained in the trusted Host receipt. All observation IDs, payload hashes, refs and action windows are checked against the controller's MCP final/history response, with monotonically ordered timestamps inside the current phase. The independent Host UI oracle fixes its own expected content and rereads the files; a Host assertion proves issuance of evidence but its caller condition is never the business oracle. It ignores Script `passed` and expected-body fields. This is a bracketed stable-state capture, not an atomic UI snapshot.

Overview text and label checks are scoped to the unique title's MaterialCardView. Other notes may legitimately contain the same body. Input-dependent taps wait for consecutive fresh observations with stable Activity, window and bounds. Label creation dismisses the IME, waits for the input dialog to disappear, then targets the uniquely named checkbox class on SelectLabelsActivity. Row controls are bound to exact LabelText inside that row of MainListView, so `a` cannot select `ab` or another row's Edit/Delete button. Window selection first chooses the highest window that is not explicitly hidden, then validates it. An unknown, disabled or missing foreground root cannot expose the background page.

Duplicate creation has an independent checkpoint proving the complete existing name is in the Add Label dialog before Save. Conflicting rename has separate checkpoints proving the original `a` value and its replacement by `ab` in the Edit Label dialog. Host UI oracles re-evaluate these exact fields against issued evidence. An input receipt that says `ok` while text remains unchanged cannot satisfy the negative case or dispatch Save. Cancellation and confirmation each archive the real Delete Label dialog and its explicit buttons. The automated rejection oracle requires the unchanged visible management page plus unchanged independent storage; it does not claim automatic OCR of transient toasts.

The independent SQLite oracle checks actual committed rows, original/edited text, unique note ID, timestamp order, prior rows, and restart preservation. List checks compare the complete ordered `body/checked/isChild/order/checkedTimestamp` objects; no durable task ID exists. The first snapshot proves four flat items and exactly Parent-B checked. Its checked timestamp must lie between that new note’s device creation and modification timestamps, then remain exactly unchanged through hierarchy edits and restart. The UI oracle independently checks four unique task markers, order and relative indentation at every editor checkpoint. Unchecked tasks belong to MainListView and remain editable/focusable with alpha 1; checked Parent-B belongs to CheckedListView and is explicitly readonly/nonfocusable with alpha 0.5. Each text must share its own Content row with a MaterialCheckBox. The current tree provides no checkbox boolean, so the actual checked value comes from the independent SQLite array. Overview checks bind the four markers to the correct card; they do not claim pixel-level checkbox or indentation verification. Both bracketing trees independently satisfy these business predicates, and editability/focus/alpha participate in the stable-state key. This does not cover parent bulk-check or check-all/uncheck-all templates. Missing/stale observations or missing screenshots are inconclusive. Wrong business values fail. A `dataOnExternalStorage=true` preference rejects this private-database collector until public-path acquisition is implemented.

The independent label database oracle preserves all prior note fields and attachments. Preparation adds `a` only to the text note and `ab` only to the list, retaining existing labels/order and allowing only those two notes' modification timestamps to advance. Rename/delete compare exact entity and all-note reference changes; deletion accounts for the service's explicit save of the hidden-label preference. Separate snapshots after duplicate, conflicting rename and cancellation must have identical canonical business values with no ignored fields. Restart compares the complete post-deletion canonical state. The oracle rejects malformed/old/unregistered snapshots and catches a partial deletion if imported data has duplicate label references; that imported-data edge case is not claimed as device-covered.

The historical source mapping uses the original `evidence-source-index.json` / `source-evidence/` and the separate frozen `labels-evidence-source-index-v2.json` / `source-evidence-labels-v2/`. The label bundle preserves 225 files: 172 indexed artifacts and 53 dependencies. It includes old crashes, rejected malformed selectors, the transitional-node failure, later successful observations, and independent SQLite evidence with their true status. It does not turn the failed deletion Intent into a completed goal. The earlier v1 increment remains unchanged. The runner requires v2 `bundleState: frozen` and verifies both bundles without reading ignored `build/` paths. Missing files, changed hashes, incorrect mappings or omitted snapshot dependencies fail source verification. Original absolute paths remain provenance strings mapped to bundle files.

Historical file integrity is separate from receipt validity: some original Host envelopes predate the canonical serialization checksum fix. The source verifier marks durable receipt checksums `not_verified` because it does not reopen those stores; it never repairs their bytes or promotes file hashes into verified receipts. A separate review may validate a particular durable store. The historical label restart has a recorded force-stop/pidof and changed capture-page epoch, but no separately saved launch transcript; an old epoch also remains in its feedback metadata. These limits stay explicit in the label source index. New runs save launch responses and acquisition transcripts. Historical sources inform authoring and are never reused for current acceptance.

The frozen `host-code-manifest.json` hashes all local CLI `bin/` files, package/lock files, the MCP client, and the actual loaded native FactStore entry/binary. The identity includes this manifest’s hash and Node version/platform. The report rechecks these file hashes after execution. This describes the executed local Host code, not a complete machine attestation.

Output includes `frozen/` source and inventories, measured MCP/adb transcripts, **14 phases / 59 UI checkpoints / 15 database snapshots**, independent oracle results, and `report/report.{json,html}`. Snapshot action sequences are counted from complete trusted Host `call_completed` history, checked against target and unique action IDs, and compared with the child's count only for consistency. `core.ok` and CLI exit status require all seven core templates, seven label assessments and the observed organization flow to pass, with valid artifacts and the ten-minute budget. Top-level `report.ok` describes the complete frozen scope and remains false while other cases are unexecuted.

Construction time in this runner measures only freezing its inputs; historical exploration, coding and debugging time is not estimated. There is no fabricated Agent/model latency or A/B/C benchmark. Reported fixed regression time includes APK preflight, Script execution, collectors, launches, independent verification, and artifact hash validation.

Run the versioned offline checks from the repository root:

```sh
node --test examples/notallyx-sample/validation/test/*.test.js
```

The frozen candidate passed **61/61** offline tests, then an independent 61/61 rerun. Coverage includes Host issuance, stale/missing/changed evidence, swapped PNGs, real R1/R4 failure fixtures, exact list structure, SQLite/WAL/private-path acquisition, external-storage rejection, label transitions across all folders, and portable dependency integrity. Real Node children execute text/list and all seven label phases with explicitly simulated providers. Noop input must fail without Save, and unknown/disabled foregrounds must dispatch no background action. Tests need no device, original build history, temporary clone, APK, or `/tmp` authoring files. They require Node with the CLI's installed native dependency and Python 3 standard-library SQLite. These offline passes are separate from R9 device acceptance.

Archived UI evidence can be re-evaluated without a device or MCP process:

```sh
node examples/notallyx-sample/validation/rerun-ui-oracles.js \
  --run-dir /absolute/path/to/the-archived-run
```

This entry verifies the selected frozen oracle and archived Host/checkpoint files against the run manifest, then independently recomputes the fixed UI expectations. It explicitly supports the historical 11- and 27-checkpoint scopes plus the new 59-checkpoint label-management scope. Missing phases remain inconclusive; Script completion and original verdicts are not inputs to business predicates. Its output is UI-only and does not claim to rerun database oracles or prove full-app acceptance.
