name: ai_app_bridge_flutter
description: Debug-only Flutter bridge for AI app inspection and control.
version: 0.2.1
repository: https://github.com/mobileAiDev/ai-app-bridge
issue_tracker: https://github.com/mobileAiDev/ai-app-bridge/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      android:
        package: io.github.mobileaidev.aiappbridge.flutter
        pluginClass: AiAppBridgeFlutterPlugin

